[package]
name = "engine"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
pokers = { version = "0.10.0", features = ["web"] }
wasm-bindgen = "0.2"

[profile.release]
opt-level = 3
lto = true
